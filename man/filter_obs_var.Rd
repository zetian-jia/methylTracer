% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met_qc.R
\name{filter_obs_var}
\alias{filter_obs_var}
\title{Filter cells and features in a methylTracer object}
\usage{
filter_obs_var(
  met,
  obs_obj = "coverage_cells",
  var_obj = "coverage_feature",
  obs_cutoff = NULL,
  var_cutoff = NULL,
  sample_name = "sample_name",
  marker_name = "marker_name",
  out_file = NULL,
  overwrite = FALSE,
  copy_coverage = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{met}{A \code{methylTracer} object.}

\item{obs_obj}{Column in \code{metObs(met)} used to filter cells
(e.g. "coverage_cells").}

\item{var_obj}{Column in \code{metVar(met)} used to filter features
(e.g. "coverage_feature").}

\item{obs_cutoff}{Numeric; keep cells with \code{obs_obj >= obs_cutoff}.
If \code{NULL}, keep all cells.}

\item{var_cutoff}{Numeric; keep features with \code{var_obj >= var_cutoff}.
If \code{NULL}, keep all features.}

\item{sample_name}{Name of the dataset under "/obs" that stores
sample IDs, passed to \code{build_met_obj()}.}

\item{marker_name}{Name of the dataset under "/var" that stores
marker IDs, passed to \code{build_met_obj()}.}

\item{out_file}{Optional path to the filtered HDF5 file. If \code{NULL},
a file named \code{filter_<original>.h5} is created in the same
directory as the original HDF5.}

\item{overwrite}{Logical; overwrite \code{out_file} if it exists.}

\item{copy_coverage}{Logical; if TRUE, subset and copy "/uns/coverage"
when present; otherwise skip it.}

\item{verbose}{Logical; print progress messages.}
}
\value{
A new \code{methylTracer} object built on the filtered file.
}
\description{
This function filters cells (columns) and features/markers (rows)
based on QC metrics stored in /obs and /var of a methylTracer object,
and writes the filtered data into a new HDF5 file. It then rebuilds
a new methylTracer object on top of the filtered file.
}
\details{
Typical use:
\itemize{
\item obs_obj = "coverage_cells"
\item var_obj = "coverage_feature"
after running compute_qc_value().
}
}
