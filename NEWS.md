# methylTracer 0.99.8

- add met_to_sce and sce_to_met function

# methylTracer 0.99.7

-   add met_to_bsseq and bsseq_to_met function

-   remove 1000 \*

-   remove build_annotation, use findLoci instead

-   fix sapce to 300000000

# methylTracer 0.99.4

-   use end (0-based) as dmr pos; fix plot dmr in CHG/CHH ylim and rect color; remove dmr_res parm in plot_dmrs function; fix scatter plot xlim and ylim; fix dens plot visable.

# methylTracer 0.99.3

-   rewrite the build count matrix function; update importFrom; rename metObs, metVar, metX; add computeObs, computeVar; speed up metObs, metVar, metX; update buildh5 rm message and HDF5Array;

# methylTracer 0.99.1

-   update namesapce; methylTracer object

# methylTracer 0.99.0

-   First commit to Bioconductor
